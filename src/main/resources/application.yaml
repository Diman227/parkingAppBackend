spring:
  application:
    name: parkingApp

  hibernate:
    dialect: org.hibernate.dialect.PostgreSQL9Dialect

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/parkingApp
    username: postgres
    password: zaq1@WSX

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    debug: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

token:
  signing:
    key: basebasebasebasebasebasebasebasebasebasebasebasebasebasebasebase